document.querySelectorAll('a[href^="#"]').forEach(link=>{link.addEventListener('click',e=>{const target=document.querySelector(link.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'});}})});const cards=document.querySelectorAll('.product-card,.premium-card');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show-card');}})},{threshold:.15});cards.forEach(card=>{card.style.opacity='0';card.style.transform='translateY(24px)';card.style.transition='all .6s ease';observer.observe(card)});document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.show-card').forEach(card=>{card.style.opacity='1';card.style.transform='none';});});const style=document.createElement('style');style.textContent='.show-card{opacity:1!important;transform:none!important}';document.head.appendChild(style);
